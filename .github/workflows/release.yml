name: release

on:
  workflow_run:
    workflows: ['version-bump']
    types: [completed]

permissions: {}

jobs:
  update-repo:
    name: update changelog
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_notes: ${{ steps.notes.outputs.release_notes }}
      tag: ${{ steps.version.outputs.tag }}
      commit_sha: ${{ steps.push.outputs.commit_sha }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: main
      - name: configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - name: extract version
        id: version
        run: |
          TAG="$(git describe --tags --abbrev=0)"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"
      - name: generate changelog
        run: scripts/generate-changelog.sh "${{ steps.version.outputs.version }}"
      - name: extract release notes
        id: notes
        run: |
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "release_notes<<$EOF" >> "$GITHUB_OUTPUT"
          sed -n "/^## \[${{ steps.version.outputs.version }}\]/,/^## \[/{ /^## \[.*\]/!p; }" CHANGELOG.md >> "$GITHUB_OUTPUT"
          echo "$EOF" >> "$GITHUB_OUTPUT"
      - name: commit changelog and retag
        id: push
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "chore: update changelog for ${TAG}"
          fi
          # Fast-forward main to include the changelog commit.
          git push origin HEAD:main
          # Move the tag to the new commit so build/publish use the updated sources.
          git tag -f -a "${TAG}" -m "Release ${TAG}"
          git push origin -f "refs/tags/${TAG}"
          echo "commit_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  build:
    name: build (${{ matrix.target }})
    needs: update-repo
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-latest
          - target: x86_64-apple-darwin
            runner: macos-latest
          - target: aarch64-apple-darwin
            runner: macos-latest
          - target: x86_64-pc-windows-msvc
            runner: windows-latest
          - target: aarch64-pc-windows-msvc
            runner: windows-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.update-repo.outputs.commit_sha }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: '1.94.0'
          targets: ${{ matrix.target }}
      - name: install aarch64 cross linker
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
      - name: build
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: cargo build --release --target ${{ matrix.target }} --bin zig
        shell: bash
      - name: package (unix)
        if: runner.os != 'Windows'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../zig-${{ matrix.target }}.tar.gz zig
          cd ../../..
          sha256sum zig-${{ matrix.target }}.tar.gz > zig-${{ matrix.target }}.tar.gz.sha256
      - name: package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path "target/${{ matrix.target }}/release/zig.exe" -DestinationPath "zig-${{ matrix.target }}.zip"
          (Get-FileHash "zig-${{ matrix.target }}.zip" -Algorithm SHA256).Hash.ToLower() + "  zig-${{ matrix.target }}.zip" | Out-File "zig-${{ matrix.target }}.zip.sha256" -Encoding ascii
      - name: upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: zig-${{ matrix.target }}
          path: |
            zig-${{ matrix.target }}.tar.gz
            zig-${{ matrix.target }}.tar.gz.sha256
            zig-${{ matrix.target }}.zip
            zig-${{ matrix.target }}.zip.sha256
          if-no-files-found: ignore

  release:
    name: github release
    needs: [update-repo, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true
      - name: create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.update-repo.outputs.tag }}
          body: ${{ needs.update-repo.outputs.release_notes }}
          files: artifacts/*
          generate_release_notes: false

  publish-crates:
    name: publish crates
    needs: [update-repo, release]
    runs-on: ubuntu-latest
    environment: crates-registry
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.update-repo.outputs.commit_sha }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: '1.94.0'
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - name: build web UI
        run: make web-build
      - name: mint crates.io token via trusted publishing
        id: crates-auth
        uses: rust-lang/crates-io-auth-action@v1
      - name: publish zig-core
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-auth.outputs.token }}
        run: cargo publish -p zig-core
      - name: wait for zig-core
        run: sleep 30
      - name: publish zig-serve
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-auth.outputs.token }}
        run: cargo publish -p zig-serve
      - name: wait for zig-serve
        run: sleep 30
      - name: publish zig-cli
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-auth.outputs.token }}
        run: cargo publish -p zig-cli

  publish-npm:
    name: publish npm
    needs: [update-repo, build]
    runs-on: ubuntu-latest
    environment: npm-registry
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.update-repo.outputs.commit_sha }}

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: install zig-workflow dependencies
        working-directory: bindings/typescript
        run: npm install

      - name: publish zig-workflow
        working-directory: bindings/typescript
        run: npm publish --access public

      - name: install zig-api-client dependencies
        working-directory: clients/typescript
        run: npm install

      - name: build zig-api-client
        working-directory: clients/typescript
        run: npm run build

      - name: test zig-api-client
        working-directory: clients/typescript
        run: npm test

      - name: publish zig-api-client
        working-directory: clients/typescript
        run: npm publish --access public
