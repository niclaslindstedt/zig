name: version-bump

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type"
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - patch
          - minor
          - major

permissions: {}

jobs:
  create-release-tag:
    name: create release tag
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main
          # A PAT (or GitHub App token) is required so that the tag push
          # triggers the release workflow. The default GITHUB_TOKEN
          # deliberately does not trigger downstream workflows.
          token: ${{ secrets.RELEASE_TOKEN }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: '1.94.0'
      - name: configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      - name: checkout main
        run: git checkout main
      - name: run release script
        run: scripts/release.sh ${{ inputs.bump != 'auto' && inputs.bump || '' }}
